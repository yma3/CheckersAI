#include "GameBoard.h"
#include <vector>

/*class Moves {
public: 
	XY origin;
	XY start;
	XY end;
	Moves(XY o, XY s, XY e) {
		origin = o;
		start = s;
		end = e;
	}
	//void doMove(GameBoard &parent);
	//void getMoves(XY cc, GameBoard &parent);



};*/